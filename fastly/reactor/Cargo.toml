[package]
name = "fastly-reactor"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
async-task.workspace = true
fastly.workspace = true
fastly-shared.workspace = true
fastly-sys.workspace = true

fastly-reactor-macros = { workspace = true, optional = true }

[features]
macros = ["dep:fastly-reactor-macros"]
