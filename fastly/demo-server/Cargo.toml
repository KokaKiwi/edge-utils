[package]
name = "fastly-demo-server"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
fastly.workspace = true

fastly-async = { workspace = true, features = ["macros"] }
